export const PUBLIC_ROUTES = ["/login", "/register", "/"]
export const PRIVATE_ROUTES = ["/dashboard", "/profile"]

// The route to redirect unauthenticated users
export const DEFAULT_LOGIN_REDIRECT = "/login"

// The route to redirect authenticated users
export const DEFAULT_AUTHENTICATED_REDIRECT = "/dashboard"

